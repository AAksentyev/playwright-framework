/** Maximum number of schema errors to keep and display for a failed schema check */
export const MAX_SCHEMA_ERRORS: number = 30;
/** Maximum number of attempts to retry an API call with the @APIRetry() decorator */
export const API_RETRY_MAX_ATTEMPTS: number = 3;
/** Time to wait before retries */
export const API_RETRY_DELAY_MS: number = 500;
